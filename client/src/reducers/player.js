import { GET_PLAYER } from '../actions/types.js'

const intialState = {
    name: 'Gabe'
}

const player = (state = intialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default player;